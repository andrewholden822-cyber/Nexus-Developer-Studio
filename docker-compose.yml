services:
  nexus-studio:
    build: .
    container_name: nexus-developer-studio
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
